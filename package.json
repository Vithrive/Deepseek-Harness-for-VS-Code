{
  "name": "deepseek-harness-vscode",
  "displayName": "DeepSeek Harness for VSCode",
  "description": "DeepSeek Harness (DSH) 的 VS Code 接入：侧边栏/标签页忠实呈现 DSH Web GUI 并自动检测启动，不影响用户对 DSH 本身的页面组织、第三方插件装配等二次开发行为；v0.7.13 起把 DSH 注册为 Copilot 聊天模型「DSH (DeepSeek Harness)、DeepSeek-V4-Pro (DSH)、DeepSeek-V4-Flash (DSH)……」——接收 Copilot 组织好的现成对话、滤除对话内容无关的 prompt、交给 DSH 二次组织、流式回写，旨在利用 DSH 强大的任务编排与工具调用能力，发挥最大性能",
  "version": "0.8.37",
  "publisher": "vithrive",
  "license": "MIT",
  "icon": "media/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/Vithrive/Deepseek-Harness-for-VS-Code.git"
  },
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./extension.js",
  "extensionKind": [
    "workspace"
  ],
  "activationEvents": [
    "onView:dsh.webview",
    "onCommand:dshPanel.resetChatMapping",
    "onCommand:dshPanel.chatStatus",
    "onStartupFinished"
  ],
  "contributes": {
    "languageModelChatProviders": [
      {
        "vendor": "dsh",
        "displayName": "DeepSeek Harness"
      }
    ],
    "configuration": {
      "title": "DeepSeek Harness Panel",
      "properties": {
        "dshPanel.url": {
          "type": "string",
          "default": "http://127.0.0.1:3080",
          "description": "DeepSeek Harness Web GUI 地址（本地或通过 SSH 隧道转发的地址）"
        },
        "dshPanel.host": {
          "type": "string",
          "default": "127.0.0.1",
          "description": "自动启动 dsh 时绑定的主机地址"
        },
        "dshPanel.port": {
          "type": "number",
          "default": 3080,
          "description": "自动启动 dsh 时监听的端口"
        },
        "dshPanel.autoStart": {
          "type": "boolean",
          "default": true,
          "description": "检测到 DSH 未运行时，是否自动执行 dsh web 启动它"
        },
        "dshPanel.autoRegisterWorkspace": {
          "type": "boolean",
          "default": true,
          "description": "是否把 VS Code 当前工作区自动注册为 DSH 工作区（新会话默认使用该工作区）"
        },
        "dshPanel.openSystemBrowser": {
          "type": "boolean",
          "default": false,
          "description": "扩展启动 dsh web 时是否保留弹系统浏览器的旧行为（默认关闭：新版 dsh web 的浏览器认证由扩展自动完成，面板内即用）"
        },
        "dshPanel.autoInstallDsh": {
          "type": "boolean",
          "default": true,
          "description": "检测到当前环境（含远程服务器）未安装 dsh 时，是否提示并代为安装（npm install -g @deepseek-ai/dsh）"
        },
        "dshPanel.dshCommand": {
          "type": "string",
          "default": "dsh",
          "description": "dsh 命令（默认从 PATH 解析；如需指定完整路径，例如 C:\\Users\\you\\AppData\\Roaming\\npm\\dsh.cmd）"
        },
        "dshPanel.killOnDispose": {
          "type": "boolean",
          "default": true,
          "description": "扩展停用或 VS Code 关闭时，是否结束由本扩展启动的 dsh 进程（不影响你手动启动的 dsh）"
        },
        "dshPanel.chatAgentPreset": {
          "type": "string",
          "default": "",
          "description": "DSH 模型会话创建时使用的 agent 预设（如 liangshen）；留空=DSH 设置里的默认预设"
        },
        "dshPanel.chatProvider": {
          "type": "string",
          "default": "",
          "description": "DSH (DeepSeek Harness) 模型条目使用的模型提供方（provider id，需已在 DSH 设置中配置，如 deepseek-official）；留空=跟随 DSH 默认模型"
        },
        "dshPanel.chatModel": {
          "type": "string",
          "default": "",
          "description": "DSH (DeepSeek Harness) 模型条目使用的模型 id（与 chatProvider 配合，如 deepseek-v4-pro）；留空=跟随 DSH 默认模型"
        },
        "dshPanel.chatTimeoutMs": {
          "type": "number",
          "default": 900000,
          "description": "单次任务最长等待时间（毫秒），超时后任务仍在 DSH 面板运行"
        },
        "dshPanel.chatSyncLookbackMin": {
          "type": "number",
          "default": 60,
          "description": "定位当前 Copilot 聊天时，扫描最近多少分钟内修改过的聊天会话文件"
        },
        "dshPanel.enableDshModel": {
          "type": "boolean",
          "default": true,
          "description": "把 DSH 注册为 VS Code 聊天模型（模型选择器出现 DSH (DeepSeek Harness)），选中即由 DSH 解题"
        },
        "dshPanel.debugModelMessages": {
          "type": "boolean",
          "default": false,
          "description": "调试：把 VS Code 发给 DSH 模型的消息结构写入工作区 .dsh-debug/ 目录"
        },
        "dshPanel.markForeignAssistant": {
          "type": "boolean",
          "default": true,
          "description": "切回 DSH 模型补发中间对话时，是否给其他模型产出的回答加「【Copilot 其他模型回答】」产地标签（帮助 DSH 区分哪些回答不是它自己生成的）；关闭则不标注"
        },
        "dshPanel.dshReasoningEffort": {
          "type": "string",
          "default": "",
          "enum": ["", "off", "low", "high", "max"],
          "description": "DSH 模型的推理档位兜底配置（reasoningEffort）：off/low/high/max；聊天界面选择优先，此配置仅在界面未选择时生效；留空=跟随 DSH 默认档位"
        }
      }
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "dsh-panel",
          "title": "DeepSeek Harness",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "dsh-panel": [
        {
          "type": "webview",
          "id": "dsh.webview",
          "name": "DeepSeek Harness"
        }
      ]
    },
    "commands": [
      {
        "command": "dshPanel.openInTab",
        "title": "DeepSeek Harness: 在标签页中打开",
        "icon": "$(open-preview)"
      },
      {
        "command": "dshPanel.openInBrowser",
        "title": "DeepSeek Harness: 在浏览器中打开",
        "icon": "$(globe)"
      },
      {
        "command": "dshPanel.refresh",
        "title": "DeepSeek Harness: 刷新",
        "icon": "$(refresh)"
      },
      {
        "command": "dshPanel.restart",
        "title": "DeepSeek Harness: 重启 dsh web",
        "icon": "$(debug-restart)"
      },
      {
        "command": "dsh.sendSelection",
        "title": "DeepSeek Harness: 发送选中内容到对话框"
      },
      {
        "command": "dshPanel.resetChatMapping",
        "title": "DeepSeek Harness: 重置 DSH 会话映射"
      },
      {
        "command": "dshPanel.chatStatus",
        "title": "DeepSeek Harness: 检查 DSH 状态"
      },
      {
        "command": "dshPanel.diagnoseModels",
        "title": "DeepSeek Harness: 诊断 DSH 模型注册表"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "dsh.sendSelection",
          "when": "editorHasSelection",
          "group": "9_cutcopypaste@100"
        }
      ],
      "view/title": [
        {
          "command": "dshPanel.refresh",
          "when": "view == dsh.webview",
          "group": "navigation"
        },
        {
          "command": "dshPanel.restart",
          "when": "view == dsh.webview",
          "group": "navigation"
        },
        {
          "command": "dshPanel.openInTab",
          "when": "view == dsh.webview",
          "group": "navigation"
        },
        {
          "command": "dshPanel.openInBrowser",
          "when": "view == dsh.webview",
          "group": "navigation"
        }
      ]
    }
  }
}
